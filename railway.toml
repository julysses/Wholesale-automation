[build]
builder = "NIXPACKS"

[build.nixpacks]
pythonVersion = "3.11"

[deploy]
startCommand = "python -m uvicorn web.app:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/api/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
