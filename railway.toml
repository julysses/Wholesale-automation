[build]
builder = "NIXPACKS"

[build.nixpacks]
pythonVersion = "3.11"

[deploy]
startCommand = "uvicorn web.app:app --host 0.0.0.0 --port ${PORT:-8000}"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
