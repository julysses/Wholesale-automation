# Scoped dependency set for the Vercel serverless function (api/index.py).
# Smaller than the root requirements.txt → smaller bundle + faster cold start.
# Root requirements.txt is kept intact for CI, Docker, and the CLI (main.py).
# Only deps reachable from the /api + /webhooks import graph are listed here.
anthropic>=0.40.0
pydantic>=2.0.0
eval_type_backport>=0.2.0
python-dotenv>=1.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
httpx>=0.27.0
tenacity>=8.2.0
phonenumbers>=8.13.0
supabase>=2.0.0
fastapi>=0.110.0
jinja2>=3.1.0
python-multipart>=0.0.9
